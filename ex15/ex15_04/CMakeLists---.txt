# add_library(mylib standard.cpp)
cmake_minimum_required(VERSION 3.10)
project(myLib)

# Use C++11 (or higher)
set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Add your source files into a library
add_library(${PROJECT_NAME} SHARED ./lib/standard.cpp)
# Optional: Emscripten-specific flags (if you want to control output)
set(CMAKE_EXECUTABLE_SUFFIX ".js")
set_target_properties(${PROJECT_NAME} PROPERTIES LINK_FLAGS "-s WASM=1")